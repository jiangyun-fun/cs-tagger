[package]
name = "cs-tagger"
version = "0.3.0"
edition = "2021"

[dependencies]
clap = { version = "4.6.1" }
rust-htslib = { version = "1.0.0", default-features = false, features = ["bzip2", "lzma", "curl"] }
