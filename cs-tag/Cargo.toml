[package]
name = "cs-tag"
version = "0.2.1"
edition = "2021"

[dependencies]
clap = { version = "4.6.1" }
rust-htslib = { version = "1.0.0", default-features = false, features = ["bzip2", "lzma", "curl"] }
